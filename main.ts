basic.forever(function () {
	let down = pins.digitalReadPin(DigitalPin.P0)
	let up = pins.digitalReadPin(DigitalPin.P3)
	let right = pins.digitalReadPin(DigitalPin.P1)
	let left = pins.digitalReadPin(DigitalPin.P2)


    
})
